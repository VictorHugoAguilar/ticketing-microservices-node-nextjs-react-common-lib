{
  "name": "@black_sheep/common",
  "version": "1.0.2",
  "description": "Shared library of ticketing",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "scripts": {
    "clean": "del ./build/*",
    "build": "npm run clean && tsc",
    "pub": "git add . && git commit -m \"Uddates\" && npm version patch && npm run build && npm publish"
  },
  "keywords": [],
  "author": "victor hugo aguilar",
  "license": "ISC",
  "devDependencies": {
    "del-cli": "^4.0.1",
    "typescript": "^4.7.4"
  }
}
